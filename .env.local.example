NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxx
CLERK_SECRET_KEY=sk_test_xxxx

# PostHog (server-side only; never expose in client)
NEXT_POST_HOG_KEY=phx_xxxx
# Optional: embed URL for session replay iframe (default: https://eu.posthog.com/embedded for EU)
# If embed fails, try: POSTHOG_EMBED_BASE=https://app.posthog.com/embedded
# POSTHOG_EMBED_BASE=https://eu.posthog.com/embedded

# Optional: OpenAI API key for LLM-based dashboard insights (Dashboard home)
# OPENAI_API_KEY=sk-xxxx

# Neon PostgreSQL (monitoring, issues, logs, posthog_events)
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Chroma Cloud (optional – syncs Neon data for vector/search). Requires OPENAI_API_KEY for embeddings.
# CHROMA_API_KEY=your-chroma-cloud-api-key
# CHROMA_TENANT=38d2507b-966b-47d3-b7a5-dd1385383481
# CHROMA_DATABASE=bugScoutAI-main
# Set to true to stop all automatic Chroma sync (ingestion + cron). Manual GET /api/db/sync-to-chroma still runs when called.
# DISABLE_CHROMA_SYNC=true

# Optional: protect cron vector-sync (Bearer token). Set CRON_SECRET and call with Authorization: Bearer <CRON_SECRET>
# CRON_SECRET=your-secret

# Issue notification emails (Integration page). When new issues are detected, an email is sent to each added address.
# Get an API key at https://resend.com. For testing use Resend's default sender (no domain verification needed):
# RESEND_API_KEY=re_xxxx
# RESEND_FROM_EMAIL=onboarding@resend.dev
